node_modules/
.webpack/
out/
dist/
*.log
.DS_Store
tests/artifacts/

# 내부 문서 안전망 — 공개 레포에 실수로 비공개 문서가 올라가지 않도록 차단.
# 내부 설계·운영 문서는 @pharmsq/ndsd-automation 레포의 docs/internal/ 에 보관.
docs/internal/
**/.local-feed/
scripts/debug-trigger.ts

# Claude Code / Playwright MCP 런타임 아티팩트 — 포털 스냅샷·콘솔 로그가
# 남을 수 있으므로 공개 레포에 절대 커밋 금지.
.claude/scheduled_tasks.lock
.playwright-mcp/
